import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.secretprotocol.game',
  appName: 'Secret Protocol',
  webDir: 'dist'
};

export default config;
